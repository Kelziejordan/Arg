Freeze these statements now:
State Refresh is a command, not a document type.
Its authoritative output is an Operational State Snapshot.
The Workspace renders operational views; it does not become the source of truth.
BOOT.md is a generated projection, never the recovery substrate.
Executive identity is a revocable runtime lease, not persistent project identity.
Resumption is valid only when the handoff contract passes.
Unknown state must block or degrade restoration, never be narrated away.
Your current candidate is the right Executive Brief projection. The actual protocol beneath it should now be frozen as a versioned state schema plus a deterministic restoration validator.
SHIP: freeze ARG Operational State Snapshot v1 as the canonical continuity object, then build the first State Refresh renderer around it.
